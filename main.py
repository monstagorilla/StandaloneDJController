#!/usr/bin/env python3

from gui import GUIApp
from kivy.core.window import Window

if __name__ == '__main__':
    app = GUIApp()
    # Window.fullscreen = True
    # Window.size = [600, 300]
    app.run()